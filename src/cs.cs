using System;

namespace cs{
    class cs{
        static void cs(){
            Console.WriteLine("Hello World");
            Console.ReadKey(); // prevent terminal emulator / cmd from closing instantly, not really required but still.
        }
    }    
}